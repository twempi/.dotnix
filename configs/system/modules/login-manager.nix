{
  services.displayManager.ly = {
    enable = true;
    settings = {
      save = true;
      load = true;

      # Animation
      animation = "matrix";
      animation_timeout_sec = 0;
    };
  };
}
